# E2E Rust port plan

This file is retained for compatibility with older links. It is not an independent plan.

Use the current documents instead:

1. [`function-gap-audit.md`](function-gap-audit.md) for current status.
2. [`e2e-capability-matrix.md`](e2e-capability-matrix.md) for detailed gaps.
3. [`e2e-replication-plan.md`](e2e-replication-plan.md) for active priorities.
4. [`live-desktop-validation.md`](live-desktop-validation.md) for real-session proof.

## Current objective

Reach real desktop native alpha: Fcitx trigger -> PipeWire capture -> native ASR -> partial/preedit -> application commit, followed by command replacement across real applications.

Do not add new milestones or progress logs here. Update the active source-of-truth document instead.
