#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
configure)
  if [[ -n "${2:-}" && -x /usr/lib/fcitx-vinpst/package-upgrade-handoff ]]; then
    /usr/lib/fcitx-vinpst/package-upgrade-handoff
  fi
  printf '%s\n' \
    'fcitx-vinpst is installed.' \
    'Enable the daemon in each desktop session with:' \
    '  systemctl --user enable --now vinpst-daemon.service' \
    'Reload Fcitx5 so the addon is discovered:' \
    '  fcitx5 -r'
  ;;
esac
