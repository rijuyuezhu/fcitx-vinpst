#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
remove | purge)
  printf '%s\n' \
    'fcitx-vinpst was removed.' \
    'User configuration, models, and cache were preserved.' \
    'Reload Fcitx5 in active desktop sessions: fcitx5 -r'
  ;;
esac
