name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - name: Ensure native test tools
        run: |
          sudo rm -f /etc/apt/sources.list.d/azure-cli.list /etc/apt/sources.list.d/microsoft-prod.list
          sudo apt-get update
          sudo apt-get install -y dbus libpipewire-0.3-dev libspa-0.2-dev libclang-dev clang pkg-config cmake g++ clang-format clang-tidy gettext libsystemd-dev libfcitx5core-dev fcitx5-modules-dev
      - name: Ensure just
        run: |
          if ! command -v just >/dev/null 2>&1; then
            cargo install just --locked
          fi
      - name: Run CI checks
        run: just ci
      - name: Run staged IME activation smoke
        run: just ime-configured-activation-smoke
      - name: Run deterministic IME e2e smoke
        run: just ime-e2e-smoke
      - name: Run adapter lifecycle smoke
        run: just addon-dbus-adapter-lifecycle-smoke
      - name: Run optional PipeWire checks
        run: just pipewire-check
